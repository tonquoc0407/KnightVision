knightvision:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('KNIGHTVISION_DUCKDB_PATH', '../../warehouse/knightvision.duckdb') }}"
      threads: "{{ env_var('KNIGHTVISION_DBT_THREADS', '4') | int }}"
